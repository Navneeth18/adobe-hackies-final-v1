# Local Dockerfile that works around network issues
# This uses a scratch approach to build without external dependencies

# Use a minimal approach - we'll build this manually
FROM scratch

# Copy everything we need
COPY . /app

# Set working directory
WORKDIR /app

# This is a placeholder - we'll run this differently
CMD ["echo", "Local Docker setup"]
